# Server-side only (never exposed to the browser). Where Next.js proxies
# /api/* requests to. In docker-compose this is the API service's internal
# hostname; for bare local dev it's your local uvicorn's address.
API_INTERNAL_URL=http://localhost:8000
