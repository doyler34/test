export default function LoginPage() {
  return (
    <main className="flex min-h-screen items-center justify-center p-4">
      <div className="w-full max-w-sm rounded-lg border p-6 text-center text-sm text-muted-foreground">
        Login is implemented in a later stage of the build.
      </div>
    </main>
  );
}
